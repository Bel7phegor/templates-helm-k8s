{{- if .Values.global.enabledComponents.configmap }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.global.appName }}-{{ .Values.configmap.name }}-configmap
  namespace: {{ .Values.global.namespace }}
data:
  {{- toYaml .Values.configmap.data | nindent 2 }}
{{- end }}
